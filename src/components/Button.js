import styled from "styled-components"

export const Button = styled.button`
  background: #666;
  color: orange;
  font-size: 2rem;
  cursor: pointer;
  transition: all 0.2s ease;
  &:hover {
    transform: scale(1.1);
  }
`
